# Copy to .env at the repo root. Read by the Supabase CLI only (see
# supabase/config.toml) — the web app's own variables live in
# apps/web/.env.example.
#
# From the Google Cloud OAuth client used for local development.
SUPABASE_AUTH_GOOGLE_CLIENT_ID=
SUPABASE_AUTH_GOOGLE_SECRET=
